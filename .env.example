# Generate a private value with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=replace-with-output-from-secrets-token_urlsafe-32
# Replace this rejected placeholder with a private code shared only with invited users.
SIGNUP_CODE=replace-with-private-signup-code
